# Zombie apocalypse

A desktop computer game in C++ inspired by __Samurai vs Zombies__.

File `levels.txt` is a configuration of zombies' counts and time intervals of their spawning.
Folder `img` contains images and `font.ttf` is a font. Other files contain a code in C++.